# Keep bundled wake-word models and short notification sounds as regular Git
# blobs. They are small enough for GitHub and must be available during Actions
# checkout before the Hugging Face sync workflow runs.
